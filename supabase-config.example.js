// Copy this file to supabase-config.js and fill in your project's values.
// Find these under Supabase Dashboard > Project Settings > API.
// supabase-config.js is intentionally NOT committed (see .gitignore) because
// it's fine to expose the anon key in a client app, but keep it out of the
// public repo history if you'd rather rotate it yourself later.

window.SUPABASE_CONFIG = {
    url: "https://YOUR-PROJECT-REF.supabase.co",
    anonKey: "YOUR-PUBLIC-ANON-KEY"
};
